@model DarboOrganizavimoPlatforma.Web.Models.AdminEditTeamViewModel

@{
    ViewData["Title"] = "Edit";
}
<!-- Content Header (Page header) -->
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6 offset-sm-3">
                <h1>Edit @Model.TeamName</h1>
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>

<!-- Main content -->
<section class="content">
    <div class="row">
        <div class="col-md-6 offset-md-3">
            <div class="card card-primary">
                <div class="card-header">
                    <h3 class="card-title">Team</h3>
                </div>
                <div class="card-body">
                    <form asp-controller="Teams" asp-action="EditTeam">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <div class="form-group">
                            <label asp-for="TeamName" class="control-label"></label>
                            <input asp-for="TeamName" class="form-control" />
                            <span asp-validation-for="TeamName" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="TeamDescription" class="control-label"></label>
                            <textarea asp-for="TeamDescription" class="form-control" rows="4"></textarea>
                            <span asp-validation-for="TeamDescription" class="text-danger"></span>
                        </div>
                        <div class="form-group ">
                            <input type="submit" value="Save Changes" class="btn btn-success float-left">
                            @if (User.IsInRole("Admin"))
                            {
                                <a asp-controller="Teams" asp-action="GetAllTeams" class="btn btn-primary float-right" style="width:auto">Back to Team List</a>
                            }
                            else
                            {
                                <a asp-controller="Manager" asp-action="CompanyTeamsList" class="btn btn-primary float-right" style="width:auto">Back to Team List</a>
                            }
                        </div>
                    </form>
                </div>
                <!-- /.card-body -->
            </div>
            <!-- /.card -->
        </div>
    </div>
</section>
<!-- /.content -->